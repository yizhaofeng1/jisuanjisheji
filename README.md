#这里是项目文件
<p>这里是项目文件，里面有演示视频、项目代码、相关说明等。 </p>
<p>后端语言是python，使用的是Django框架，链接了数据库，但是当时为了保证省赛在其他电脑上运行，也使用了链接csv文件的方式，但里面是有链接数据库的代码但我注释掉了。 </p>
<p>作品当中，使用了diffusers的stable-v1.5模型进行文生图，所以有torch框架，使用了opneai库调取了deepseak的接口实现了一些功能比如大规模文本处理和提取信息，英文翻译（为了diffusers的使用）。 </p>
<p>注意：diffusers我自己跑的是使用本地模型，下载到本地模型的代码写的，项目上传的应该是链接网络再下载模型，模型比较大，需要魔法，而且注意流量。</p>
